#include <stdio.h>
#include <math.h>
#include "make_me_win.h"

void make_me_win(int a, int b, int c, int d, int e, int f) {
    printf("%d %.2f %.2f %.2f %.2f %d\n", a, pow(a, 0.5), pow(b, 0.5), pow(c, 0.5), pow(d, 0.5), e, f);
}
